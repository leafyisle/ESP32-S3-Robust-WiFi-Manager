#ifndef SECRETS_H
#define SECRETS_H

// 将此文件复制并重命名为 secrets.h，然后填入真实的 WiFi 信息。
// secrets.h 已被 .gitignore 忽略，不会被提交到版本库。

constexpr const char* WIFI_SSID = "YOUR_SSID_HERE";
constexpr const char* WIFI_PASSWORD = "YOUR_PASSWORD_HERE";

#endif